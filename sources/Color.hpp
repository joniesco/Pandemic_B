#pragma once
#include <vector>
#include <string>

using namespace std;

namespace pandemic{
    enum Color{
        Black,
        Blue,
        Red,
        Yellow
    };

     const std::vector<std::string> colors = {"Black","Blue","Red","Yellow"};
};